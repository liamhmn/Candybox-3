/**
 * Contains methods to have elements which are which between versions.
 */
module Version {

    export function getSingular(): string {
        return " " + "candy";
    }

    export function getPlural(): string {
        return " " + "candies";
    }

}
